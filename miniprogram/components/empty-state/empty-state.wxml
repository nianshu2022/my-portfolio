<view class="empty-state">
  <text class="empty-icon">{{icon}}</text>
  <text class="empty-title">{{title}}</text>
  <text class="empty-desc" wx:if="{{desc}}">{{desc}}</text>
  <view class="empty-btn" wx:if="{{btnText}}" bindtap="onBtnTap">
    <text>{{btnText}}</text>
  </view>
</view>
