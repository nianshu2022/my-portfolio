"use client";

import { useEffect } from "react";

export default function ServiceWorkerRegister() {
    useEffect(() => {
        // Service Worker 已按需禁用（见之前版本记录）。如有需要，以后可重新启用。
    }, []);

    return null;
}
